; peers（@deepseek-ai/cordis、@deepseek-ai/dsh-tools）由 scripts/build.sh 从已安装的
; @deepseek-ai/dsh 包 junction 链接（编译期），运行期由 profile node_modules 提供。
; 阻止 npm 自动安装 peerDependencies —— @deepseek-ai 内部依赖链可能未发布到 registry，
; 自动解析会 404 或装出与宿主不同源的副本。
legacy-peer-deps=true
