# Changelog

## 0.3.9 — 2026-09-11

- **取数时机改为「打开即刷新一次，之后只手动刷新」**：
  - 打开面板 → `force=1` 强制重建一次（受宿主 20s 最小重建间隔保护，反复开关不会连击）；
  - 切粒度 → 被动读（同一份索引换聚合口径，不重建）；
  - 手动 ⟳ / 重试 → `force=1`；
  - 其余时间**零请求**（实测：面板开着静默 60 秒，新增请求 0）。
- 头部新增「更新中…」状态：重建在跑时显示琥珀点 + `数据截止 X · N 分钟前 · 更新中…`，
  拿到新数据后自动去掉 —— 让"我按的刷新到底有没有生效"一眼可见。
# Changelog

## 0.3.8 — 2026-09-11

- **被动新鲜窗口 5 → 10 分钟**（`FRESH_MS`）：无人读接口时插件完全静默（实测 95 秒零日志、
  零重建）；有读时才按窗口决定是否重建。
- **单会话索引 TTL 10 → 30 分钟**（`INDEX_TTL_MS`）。它必须明显长于 `FRESH_MS`：否则每轮
  重建都会发现上轮留下的索引刚好过期 → 退化成"每次都全量重读"（这正是重建要跑 50~110s
  的主因）。放宽后常规重建降到 ~2.5s（首次冷启动仍可能上百秒）。
- 新增 `refresh.inFlight`：修复「按了 ⟳ 却看不到新数字」——重建进行中时被动读会返回窗口内
  的旧数据（`stale=false`），客户端只看 `stale` 会误判为"已完成"而停止重取。现在
  `stale || inFlight` 都继续退避重取，直到真拿到新数据（实测 ⟳ 后 48.5s 收敛到新 cutoff）。
- 新增 `refresh.lastError` + 失败不再静默：重建抛错时记录原因（成功即清空），
  冷启动阶段失败会以 400 带 message 返回，而不是假装成功。
# Changelog

## 0.3.7 — 2026-09-11

- **修复「一直在刷新 / 重建太频繁」（宿主侧）**：被动新鲜度窗口 `FRESH_MS` 由
  **60 秒放宽到 5 分钟**。原值意味着任何一次读接口（面板打开、客户端重取、
  `token_usage` 工具）在 60 秒后都会触发一次全量重建索引；而一次重建在会话多、
  日志大的机器上要跑 50~110 秒 —— 实测表现就是「持续在重建」。现在实测：
  6 分钟窗口内只重建 2 次，两次之间 260 秒全部是 6~11ms 的缓存命中。
- 新增 `REINDEX_FLOOR_MS = 20s`：两次重建的最小间隔，连点 ⟳ 也不会反复触发全量。
- 新增 `force` 语义（`?force=1`）：面板 ⟳ / 重试按钮可忽略新鲜度窗口立即重建，
  仍受最小间隔保护；被动读一律走窗口。
- **客户端重取改为退避 + 静默 + 不可见不轮询**：间隔 5s→10s→20s→40s→60s（上限 8 次），
  重取不再把面板打回「加载中」（保留当前数据，只在拿到新数据时替换），并用
  IntersectionObserver 在面板不可见（切走/收起侧栏）时停止重取、重新可见时补一次。
- 实测：稳态下面板打开后 150 秒内只有 2 个请求（summary + models），零轮询。
# Changelog

## 0.3.6 — 2026-09-10

- **修复面板永远「加载中…」（真正的病根）**：`ensureData` 在索引不新鲜时会
  `await refreshOnce()` —— 即同步等待一次**全量重建索引**。在会话数量多、日志体量大的
  环境下，一次冷索引可能跑数分钟（其中读取失败的会话会被跳过），期间
  `/token-lens/api/summary` 请求全部挂起、无响应，面板自然永远转圈。
  现在：已有落盘索引时最多等 1.5s，超时即用旧索引返回（`stale:true`），重建在后台继续；
  只有真·首次（无任何索引）才同步等待。
- 修复 `warm()` 绕过单飞锁：冷启动时它和 `ensureData` 会**并发跑两遍全量刷新**，
  IO 翻倍、更慢。现在两者共用同一把锁（`startRefresh`）。
- 面板侧：遇到 `stale` 响应会每 15s 自动重取（上限 16 次），后台重建完成后自动换成新数据。
- 入口：会话头部按钮改为**调用宿主右侧栏**（`ctx.sidebarRight.openTab('token-lens')`）——
  由内核拉开侧栏并切到本页签，不再依赖自定义浮层；浮层只作为宿主无该服务时的兜底。
  另修 `BUILTIN_TAB_KIND` 未定义导致该路径回退的 bug。

## 0.3.5 — 2026-09-10

- **悬浮卡片改用顶层（top layer）原生 `<dialog showModal()>`**：此前的 `body` 内
  `position: fixed` 浮层会被任何带 `transform` / `filter` / `backdrop-filter` 的祖先
  当成包含块（皮肤/动效类插件很容易加），一旦被劫持就会渲染到视口外——表现正是
  "按钮点得动、但页面不出现"。dialog 走浏览器顶层渲染，免疫堆叠上下文与包含块，
  Esc（原生 cancel）与遮罩点击（`::backdrop`）也由原生语义承担。
- 防连点：同一 400ms 窗口内的第二次点击不再立刻关闭刚开的卡片（连点/宿主事件
  冒泡导致的"开了又关"）；开/关各打一行 `[token-lens]` 日志便于自查。

## 0.3.4 — 2026-09-10

- 新增**会话头部入口**（官方槽位 `conversation.session.header.actions`，与内置的
  任务/定时按钮同列）：28px 圆形透镜图标按钮，点开**悬浮卡片**（Esc / 点遮罩关闭）。
  这是插件的主入口；右侧栏页签保留作为"停靠"形态。
- 补齐**此前缺失的悬浮层样式**：`.tl-overlay` / `.tl-modal` / `.tl-close` / `.tl-entry*`
  在 PANEL_CSS 里从来没有定义过（旧版兜底入口点开是个没样式的裸层），现按宿主
  `--dsw-alias-*` 变量补齐，深浅色自动跟随。
- 兜底链收敛为：会话头部入口（恒在）→ 右侧栏页签 / better-sidebar 页签 →
  `sidebar.footer.action` 按钮（仅在宿主连头部槽位都没有时才出现）。
- 回归升级到 13 项：新增「头部槽位可用 → 兜底永不注册」的 B2 场景，其余场景改为
  按槽位精确断言（不再把头部入口误算成兜底）。

## 0.3.3 — 2026-09-10

- 适配 DSH 0.1.5：右侧栏收进内核、第三方 `dsh-better-sidebar` 被移除，插件此前
  退化到左侧栏兜底按钮（正好压在「设置」上面）。现优先注册进**内核右侧栏**：
  `ctx.inject(['sidebarRightTabs']) → registry.register({id, kind, title, guide})`
  + 槽位 `sidebar.right.pane.tab` / `sidebar.right.pane.tab.title`（entryKey = definition.id，
  契约对齐内置生产者 `dsh-client-ui-sidebar-files`）。用户从右侧栏引导胶囊点开。
- 入口兜底链：内核右侧栏（0.1.5+）→ better-sidebar（旧版 DSH）→ `sidebar.footer.action` 按钮。
- 修复：`ctx.slots.register/inject` 摘下来调用丢 `this`（`Cannot read properties of
  undefined (reading 'ctx')`）——改为绑定调用。

## 0.3.2 — 2026-09-08

- 修复：左侧边栏底部常驻「Token Lens」按钮（入口位置错乱）——兜底入口
  （`sidebar.footer.action`）原先只靠「标签注册成功后 dispose」撤除，依赖两个
  回调的先后顺序；footer 槽位声明晚于标签注册时 dispose 落空，按钮注册后再无
  人撤。改为状态驱动（`useSyncExternalStore` + 模块级 tabRegistered），与顺序无关。
- 修复：同 id 标签已在册（重复加载/热重载残留）不再被当成注册失败——视为就位
  并撤掉兜底入口；真失败时才保留兜底，并把原因打 `console.error`。
- 新增：`tests/smoke-client-entry.mjs` —— 四种时序（正常/竞态/重复/真失败）的
  入口退场契约回归，直接跑构建产物，`node tests/smoke-client-entry.mjs`。

## 0.3.1 — 2026-08-24

- 修复：趋势图峰值标注在最高柱时被画布上沿裁切 → 标注移到圆点侧面，靠右自动换边
- 新增：「效率速览」卡（均摊每回合 tokens / 输出占比 / 活跃天数 / 头部会话集中度）
- 修复：两列布局由视口媒体查询改为容器查询——窄抽屉不再误触发两列

## 0.3.0 — 2026-08-23

- UI 重设计「柔和卡片」：Apple 健康式大圆角卡片 × Plausible 单列排布
- 配色基底切换到宿主 `--dsw-alias-*` 设计变量，深浅色自动跟随界面

## 0.2.1 — 2026-08-23

- 修复：`/models` 的 from/to 接受纯数字 epoch（面板首屏 400 事故回归）

## 0.2.0 — 2026-08-23

- 入口迁移：注册进 dsh-better-sidebar 标签系统（与终端/浏览器并列），原侧边栏按钮降级为兜底

## 0.1.0 — 2026-08-23

- 入口调整：从会话头部视图标签改为全局入口 + 全屏悬浮层

## 0.0.1 — 2026-08-22

- 首个可用版本：聚合引擎（四桶口径 · 防重计 · 本地时区）、增量缓存、HTTP API、`token_usage` 工具、Web 面板
